# Stripe 配置
# 获取方式：https://dashboard.stripe.com/test/apikeys (确保在 Test mode)
STRIPE_SECRET_KEY=sk_test_your_secret_key_here

# Stripe 价格 ID（可选）
# 如果不设置，系统会自动使用一次性价格（€15 或 €39）
# 获取方式：在 Stripe Dashboard 创建产品后获取 Price ID
STRIPE_PRICE_BASIC=price_xxx
STRIPE_PRICE_EXPERT=price_xxx

# Stripe Webhook Secret（可选，用于验证 webhook 签名）
# 获取方式：配置 webhook 后从 Stripe Dashboard 复制
STRIPE_WEBHOOK_SECRET=whsec_xxx

# Webhook 重试令牌（用于手动重试）
WEBHOOK_RETRY_TOKEN=change_me

# 前端 URL
FRONTEND_URL=http://localhost:3001

# 数据库配置（可选，默认使用 SQLite）
DATABASE_URL=sqlite:///./payments.db

# API Base URL（前端使用）
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Sentry（后端）
SENTRY_DSN=
SENTRY_ENV=local

# Sentry（前端）
NEXT_PUBLIC_SENTRY_DSN=
NEXT_PUBLIC_SENTRY_ENV=local
